export declare const dialog: {
  readonly action: {
    readonly gap: 'var(--dialog-action-gap)';
  };
  readonly backgroundColor: 'var(--dialog-background-color)';
  readonly borderRadius: 'var(--dialog-border-radius)';
  readonly content: {
    readonly gap: 'var(--dialog-content-gap)';
  };
  readonly gap: 'var(--dialog-gap)';
  readonly padding: 'var(--dialog-padding)';
  readonly width: 'var(--dialog-width)';
};
