import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './DetailText.stories';
import StorybookLink from '../../../../../shared/storybook/StorybookLink';
import { DetailText } from './DetailText';
import { Flex } from '../Flex/Flex';
import { ViewMarkdownLink } from '../../docs/storybook-components/ViewMarkdownLink';

<Meta title="Typography / DetailText" />

<Flex justifyContent="between" alignItems="baseline">
  # DetailText
  <ViewMarkdownLink to="components/detail-text" />
</Flex>

<Canvas of={Stories.KitchenSink} sourceState="none" />

<Canvas of={Stories.Playground} sourceState="hide" />

## Alternatives

- <StorybookLink to="typography-bodytext">BodyText</StorybookLink> - For body text
- <StorybookLink to="typography-strong">Strong</StorybookLink> - For strong importance
- <StorybookLink to="typography-em">Em</StorybookLink> - For emphasis
- <StorybookLink to="typography-heading">Heading</StorybookLink> - For heading-level text

## Size

The size prop is responsive, so you can render different sizes at different breakpoints.

```tsx
<DetailText size={{ mobile: 'sm', tablet: 'md', desktop: 'lg'}}>{...}</DetailText>
```

| Size | Font-size | Line-height | Letter-spacing |
| ---- | --------- | ----------- | -------------- |
| sm   | 14px      | 18px        | -0.42px        |
| md   | 16px      | 24px        | -0.48px        |
| lg   | 18px      | 22px        | -0.54px        |
| xl   | 20px      | 22px        | -0.60px        |
| 2xl  | 24px      | 32px        | -0.72px        |
| 3xl  | 30px      | 36px        | -0.90px        |
| 4xl  | 36px      | 36px        | -1.08px        |

<Canvas of={Stories.TextSizes} />

## Color

The text color of `DetailText` can be set to either `text`, `valid` or
`invalid`. These colours will be overridden by the `inverted` prop.

## Inverted

When using `BodyText` on a darker background, specifically `uwPurple` &
`darkPurple`, use the `inverted` prop to ensure colour contrast.

<Canvas of={Stories.InvertedText} />

## API

This component is based on the `span` element and supports the following common props:

- <StorybookLink to="common-props-margin">Margin</StorybookLink>
- <StorybookLink to="common-props-text">Text transform</StorybookLink>
- <StorybookLink to="common-props-text">Text align</StorybookLink>
- <StorybookLink to="common-props-text">Text wrap</StorybookLink>

<ArgTypes of={DetailText} exclude={'margin*|textTransform|textAlign|textWrap|wrap'} />
