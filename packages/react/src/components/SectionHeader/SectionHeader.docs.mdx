import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './SectionHeader.stories';
import { SectionHeader } from './SectionHeader';
import { StorybookLink } from '@utilitywarehouse/hearth-storybook-utils';

import { MarkdownDocHeader } from '../../../docs/storybook-components/MarkdownDocHeader';

<Meta title="Components / SectionHeader" />

<MarkdownDocHeader title="SectionHeader" to="components/section-header" />

Use the `SectionHeader` component to help define and separate content within a
screen. It provides structure, clarity, and improves page scannability for
users.

<Canvas of={Stories.Playground} sourceState="hide" />

## Trailing content

Use the `trailingContent` prop to include a `Link`, `Button` or `Badge` component.

```tsx
import { Link, SectionHeader } from '@utilitywarehouse/hearth-react';
import { ChevronRightSmallIcon } from '@utilitywarehouse/hearth-react-icons';

[...]

<SectionHeader
  {...args}
  trailingContent={
    <Link>
      Link text
      <ChevronRightSmallIcon />
    </Link>
  }
/>
```

<Canvas of={Stories.WithBadge} />
<Canvas of={Stories.WithButton} />

## API

This component is based on the `div` element and supports the following common props:

- <StorybookLink to="common-props-margin">Margin</StorybookLink>

<ArgTypes of={SectionHeader} exclude={'margin*'} />
