import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';

import * as Stories from './Label.stories';
import StorybookLink from '../../../../../shared/storybook/StorybookLink';
import { Label } from './Label';

import { MarkdownDocHeader } from '../../../docs/storybook-components/MarkdownDocHeader';

<Meta title="Typography / Label" />

<MarkdownDocHeader title="Label" to="components/label" />

<Canvas of={Stories.Playground} sourceState="hide" />

## API

This component is based on the `label` element, and supports the following common props:

- <StorybookLink to="common-props-margin">Margin</StorybookLink>
- <StorybookLink to="common-props-text">Text transform</StorybookLink>

<ArgTypes of={Label} exclude={'margin*|textTransform'} />
