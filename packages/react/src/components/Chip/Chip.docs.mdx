import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as Stories from './Chip.stories';
import { Chip } from './Chip';
import { MarkdownDocHeader } from '../../../docs/storybook-components/MarkdownDocHeader';
import { StorybookLink } from '@utilitywarehouse/hearth-storybook-utils';

<Meta title="Components / Chip" />

<MarkdownDocHeader title="Chip" to="components/chip" />

Chip is a compact, interactive element that represents an input, attribute, or
filter. It lets users see active selections at a glance and remove them with
a single click.

<Canvas of={Stories.KitchenSink} sourceState="show" />

## Usage

Render a Chip for each active selection, and remove it from your state when
the user clicks it.

```tsx
<Chip onClick={() => removeFilter(filter)}>{filter.label}</Chip>
```

<Canvas of={Stories.Playground} sourceState="show" />

## Removable

Chip always renders a close icon to signal that it can be removed. Attach an
`onClick` handler to update your application state when the user clicks it.

<Canvas of={Stories.Removable} sourceState="show" />

## Disabled

Set `disabled` to prevent a Chip from being removed.

<Canvas of={Stories.Disabled} sourceState="show" />

## API

This component is based on the `button` element and supports the following
common props:

- <StorybookLink to="common-props-margin">Margin</StorybookLink>

<ArgTypes of={Chip} include={'children|disabled|onClick'} exclude={'aria-*'} />
