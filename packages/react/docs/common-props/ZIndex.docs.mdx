import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { Box } from '../../src/components/Box/Box';
import { MarkdownDocHeader } from '../storybook-components/MarkdownDocHeader';

<Meta title="Common Props / Z-Index" />

<MarkdownDocHeader title="Z-Index" to="docs/common-props/z-index" />

The `zIndex` prop sets the `z-index` value, it is responsive but does not
use any token values.

```tsx
<Box zIndex="1" />
<Box zIndex={{ mobile: "1", desktop: "-1" }}/>
```

<ArgTypes of={Box} include={'zIndex'} sort="requiredFirst" />
