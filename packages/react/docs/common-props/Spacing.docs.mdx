import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { Flex } from '../../src/components/Flex/Flex';
import { MarkdownDocHeader } from '../storybook-components/MarkdownDocHeader';

<Meta title="Common Props / Spacing" />

<MarkdownDocHeader title="Spacing" to="docs/common-props/spacing" />

The `spacing` prop is not responsive, instead it provides a predetermined set
of responsive steps for spacing _between_ elements. These will be overridden by
any usage of the `Flex` or `Grid` gap props.

```tsx
<Flex spacing="lg" />
```

<ArgTypes of={Flex} include={'spacing'} sort="requiredFirst" />
