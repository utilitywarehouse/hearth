{
  "name": "@utilitywarehouse/hearth-json-assets",
  "version": "0.4.1",
  "description": "Hearth JSON assets",
  "scripts": {
    "clean:lib": "rm -rf lib",
    "gen:script": "node ./scripts/generate-json-assets.js",
    "generate": "pnpm clean:lib && pnpm gen:script"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "fs-extra": "^11.1.1",
    "node-fetch": "^2.7.0"
  },
  "files": [
    "lib"
  ]
}
