{
  "name": "@utilitywarehouse/hearth-json-assets",
  "version": "0.0.2",
  "description": "Hearth JSON assets",
  "scripts": {
    "clean:lib": "rm -rf lib",
    "generate": "pnpm clean:lib && node ./scripts/generate-json-assets.js"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "fs-extra": "^11.1.1",
    "node-fetch": "^2.7.0"
  },
  "files": [
    "lib"
  ]
}
