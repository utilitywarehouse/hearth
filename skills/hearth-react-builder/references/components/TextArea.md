# TextArea

Multi-line text input. Same label/validation props as [`TextInput`](TextInput.md).

```tsx
import { TextArea } from '@utilitywarehouse/hearth-react';

<TextArea
  label="Message"
  placeholder="Tell us about your issue..."
  helperText="Maximum 500 characters"
  rows={4}
/>
```
