import { Meta } from '@storybook/addon-docs/blocks';
import { version } from '../../../packages/svg-assets/package.json';
import { svgAssets } from '../utils/svg-assets';

<Meta title="SVG Assets" />

# Hearth SVG Assets

Current version: v{version}

These are static SVG assets for storytelling and brand visuals.

## Available assets ({svgAssets.length})

<ul style={{ display: 'flex', flexDirection: 'column' }}>
  {svgAssets
    .map(({ path }) => path)
    .sort()
    .map(name => (
      <li>{name}</li>
    ))}
</ul>
