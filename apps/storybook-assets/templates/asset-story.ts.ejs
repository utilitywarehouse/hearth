/* Don't edit this file directly, it was auto generated */
import type { Meta, StoryObj } from '@storybook/react';
import <%-asset.name%>Src from '@utilitywarehouse/hearth-svg-assets/lib/<%-asset.path%>';

const meta: Meta = {
  title: 'Individual Assets/<%-asset.name%>',
};

export default meta;
type Story = StoryObj;

export const <%-asset.name%>Story: Story = {
  name: '<%-asset.name%>',
  render: () => (
    <img src={<%-asset.name%>Src as unknown as string} alt="<%-asset.name%>" style={{ maxWidth: 320 }} />
  ),
};
