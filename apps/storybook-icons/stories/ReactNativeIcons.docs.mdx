import { icons } from '../utils/icons';
import { Meta, ArgTypes } from '@storybook/blocks';
import { version } from '../../../packages/react-native-icons/package.json';
import * as Stories from './Icons.stories';

<Meta title="React Native Icons" />

# React Native Icons

Current version: v{version}

This package provides the UW Icons as optimized React Native components, generated
from the `svg-icons` package.

## Contents

- [Contents](#contents)
- [Installation](#installation)
- [Props](#props)
- [Icon sizes](#icon-sizes)
- [Icon colours](#icon-colours)
- [Icon naming](#icon-naming)
- [Usage](#usage)
- [Available Icons](#available-icons)
  - [Small](#small)
  - [Medium](#medium)
  - [Large](#large)

## Installation

```console
yarn add @utilitywarehouse/react-native-icons
```

## Props

Each icon component supports the full range of [SVG attributes](https://developer.mozilla.org/en-US/docs/Web/SVG/Element/svg#attributes) as well as the following props.

<ArgTypes of={Stories} />

## Icon sizes

There are 3 sizes of icons:

- Small: 16px
- Medium: 24px
- Large: 48px

## Icon colours

The icon components don't have any predefined colours. The rendered SVG fill
colour is `currentColor` by default. You can change the icon colour using the
`color` prop, passing in any valid colour string. We recommend using the colour values from the UW
`colour-system` package.

## Icon naming

The icon names are structured as:

> `[Name][Size][Variant]Icon.svg`

- `name` - Name of the icon from [Figma]('https://www.figma.com/file/WDFaQF9EMtS7MjuIpjDVpf/UW-Icons?node-id=27-230&t=PKen5tLHUHUzchVS-0') that is all lowercased and hyphen separated.
- `size` - Size of the icon that is one of small (16)/medium (24)/large (48). Note that some icons do not have all sizes available yet. Our designers are working to add missing ones to complete the collection.
- `variant` - Optional variant of the icon that is one of `outlined` or `filled`. Not all icons will have variants.

## Usage

```tsx
import { AddMediumIcon } from '@utilitywarehouse/react-native-icons';
import { colors } from '@utilitywarehouse/colour-system';

[...]

<AddMediumIcon color={colors.green700}>
```

## Available Icons

There are currently {icons.length} icons available.

### Small

<ul style={{ display: 'flex', flexDirection: 'column' }}>
  {icons
    .filter(({ reactName }) => reactName.includes('Small'))
    .map(({ reactName }) => reactName)
    .sort()
    .map(name => (
      <li>{name}</li>
    ))}
</ul>

### Medium

<ul style={{ display: 'flex', flexDirection: 'column' }}>
  {icons
    .filter(({ reactName }) => reactName.includes('Medium'))
    .map(({ reactName }) => reactName)
    .sort()
    .map(name => (
      <li>{name}</li>
    ))}
</ul>

### Large

<ul style={{ display: 'flex', flexDirection: 'column' }}>
  {icons
    .filter(({ reactName }) => reactName.includes('Large'))
    .map(({ reactName }) => reactName)
    .sort()
    .map(name => (
      <li>{name}</li>
    ))}
</ul>
