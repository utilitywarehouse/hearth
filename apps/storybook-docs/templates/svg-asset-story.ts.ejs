/* Don't edit this file directly, it was auto generated */
import type { Meta, StoryObj } from '@storybook/react';
import <%-asset.name%> from '@utilitywarehouse/hearth-svg-assets/lib/<%-asset.path%>';

const meta: Meta = {
  title: 'Assets / SVG / <%-asset.name%>',
};

export default meta;
type Story = StoryObj;

export const <%-asset.name%>Story: Story = {
  name: '<%-asset.name%>',
  render: () => (
    <img src={<%-asset.name%> as unknown as string} alt="<%-asset.name%>" style={{ maxWidth: 320 }} />
  ),
};
