import { Meta } from '@storybook/addon-docs/blocks';
import { AllTokensTable } from './AllBrowserTokensTable';

<Meta title="Hearth Tokens/ Browser and CSS Tokens" />

# Browser and CSS Tokens

Every token exported from `@utilitywarehouse/hearth-tokens/browser`, with its CSS custom property and resolved raw value.
Token names use dot-separated keys reflecting the object structure (e.g. `color.blue.500`).

<div className="sb-unstyled">
  <AllTokensTable />
</div>
