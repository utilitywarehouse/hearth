import { Meta } from '@storybook/addon-docs/blocks';
import { AllTokensTable } from './AllJsTokensTable.tsx';

<Meta title="Tokens / JS Tokens" />

# JS Tokens

Every token exported from `@utilitywarehouse/hearth-tokens/js`, displayed with its raw value.
Token names use dot-separated keys reflecting the object structure (e.g. `color.blue.500`).

<div className="sb-unstyled">
  <AllTokensTable />
</div>
