import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './Container.stories';
import { DocsHeader } from './storybook-components/DocsHeader';

<Meta title="Layout / Container" />

<DocsHeader componentName="Container" stories={Stories} />

## Playground

<Canvas of={Stories.Playground} sourceState="shown" />
<Controls of={Stories.Playground} />

## Alternatives

- [Box](?path=/docs/layout-box--docs) - For primitive styling and layouts
- [Grid](?path=/docs/layout-grid--docs) - For grid layouts
- [Flex](?path=/docs/layout-flex--docs) - For stacked, inline or flexbox based layouts

## Semantic HTML

By default `Container` renders a `div` element, this can be customised using the
`asChild` prop.

```tsx
<Container asChild>
  <main>...</main>
</Container>
```

## Max width

`Container` does not define it's own max-width, however you can apply your
app-specific max-width, usually only at the `desktop` breakpoint.

```tsx
<Container maxWidth={{ desktop: '1096px' }}>{...}</Container>
```

## Align

By default `Container` aligns it's content in the center, you can change this with the `align` prop.

```tsx
<Container align='start'>{...}</Container>
```

## Spacing

As well as the `gap` prop for flexbox spacing, there is a `spacing` prop which
uses fewer steps and is already optimised for responsive design. This prop will
be overridden by the `gap` prop.

```tsx
<Container spacing="lg">
```

## Show/hide content

The `display` prop is responsive, you can use this to show or hide content responsively.

```tsx
<Container display={{ mobile: 'none', tablet: 'flex' }}>hide on mobile screens</Container>
```

## API

This component is based on the `div` element and supports the following common props:

- Margin
- Size
- Background colour

| Prop             | Type                                                                                         |
| ---------------- | -------------------------------------------------------------------------------------------- |
| `asChild`        | `boolean`                                                                                    |
| `align`          | `Responsive<"start" \| "center" \| "end">`                                                   |
| `display`        | `Responsive<"none" \| "flex" \| "inline-flex">`                                              |
| `direction`      | `Responsive<"row" \| "column" \| "row-reverse" \| "column-reverse">`                         |
| `alignItems`     | `Responsive<"start" \| "center" \| "end" \| "baseline" \| "stretch">`                        |
| `alignContent`   | `Responsive<"start" \| "center" \| "end" \| "stretch" \| "around" \| "between" \| "evenly">` |
| `justifyContent` | `Responsive<"start" \| "center" \| "end" \| "between" \| "around" \| "evenly">`              |
| `wrap`           | `Responsive<"wrap" \| "nowrap" \| "wrap-reverse">`                                           |
