import { Meta, Canvas, Controls } from '@storybook/blocks';

import * as Stories from './PasswordInput.stories';

import { DocsHeader } from './storybook-components/DocsHeader';

<Meta title="Components / PasswordInput" />

<DocsHeader componentName="PasswordInput" stories={Stories} />

## Playground

<Canvas of={Stories.Playground} sourceState="shown" />
<Controls of={Stories.Playground} />

## Alternatives

- [TextInput](?path=/docs/components-textinput--docs) - For text
- [SearchInput](?path=/docs/components-searchinput--docs) - For search

## Label

A label is required, this should be clear and descriptive to guide the user.

## Disabled and Read-only

The component uses the `aria-disabled` and `read-only` attributes together to
indicate a disabled input, rather than `disabled`, so that they are still
focusable when using the keyboard. This means that screen readers are still
able to find the input, with the insight that it is disabled and that there is
perhaps an action which needs to be taken, rather than not knowing there is an
input there at all.

Read-only inputs can not be edited, but remain focusable, and should only be
used readonly when presenting already submitted information.. They differ from
disabled inputs, as there may be actions a user can take to activate a disabled
input, but read-only fields should stay undeditable in the same view.

<Canvas of={Stories.DisabledAndReadOnly} />

## Validation

The input `validationStatus` can be set to either `"valid"` or `"invalid"` to
indicate the status. This status must be accompanied by a validation text to
explain the reason for the status.

<Canvas of={Stories.Validation} />

## Autocomplete

Use the `autocomplete` attribute on password inputs to help users complete forms
faster.

Autocomplete indicates to browsers and password managers what kind of password
is needed so it can be entered for the user.

Set the autocomplete attribute to `new-password` if the user is creating a
password. Otherwise, use `current-password`.

## API

This component is based on the `input` element.

| Prop               | Type                   |
| ------------------ | ---------------------- |
| `label`            | `string`               |
| `supportingText`   | `string`               |
| `validationStatus` | `"valid" \| "invalid"` |
| `validationText`   | `string`               |
