import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './Breadcrumbs.stories';

import { DocsHeader } from './storybook-components/DocsHeader';

<Meta title="Components / Breadcrumbs" />

<DocsHeader componentName="Breadcrumbs" stories={Stories} />

## Playground

<Canvas of={Stories.Playground} sourceState="shown" />
<Controls of={Stories.Playground} />

## Current page

Use the `currentPage` prop to indicate the current page.

## Inverted

Use the `inverted` prop to adjust colours for use on darker brand backgrounds.

<Canvas of={Stories.Inverted} />

## Breadcrumbs API

This component is based on the `nav` element and supports the following common props:

- Margin

## BreadcrumbItem API

This component is based on the `Link` component.

| Prop             | Type      |
| ---------------- | --------- |
| `currentPage`    | `boolean` |
| `inverteinverted | `boolean` |
