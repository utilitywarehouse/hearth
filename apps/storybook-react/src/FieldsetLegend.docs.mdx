import { Meta, Canvas, ArgTypes } from '@storybook/blocks';

import * as Stories from './FieldsetLegend.stories';

import { DocsHeader } from './storybook-components/DocsHeader';

<Meta title="Components / FieldsetLegend" />

<DocsHeader componentName="FieldsetLegend" stories={Stories} />

<Canvas of={Stories.Playground} />

## Custom label

If necessary, perhaps for accessibility reasons, you can render a different element as a child.

<Canvas of={Stories.CustomElement} />

## API

This component is based on the `legend` element, and supports common margin props.
