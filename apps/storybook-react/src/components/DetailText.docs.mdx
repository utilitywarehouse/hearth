import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './DetailText.stories';

<Meta title="Typography / DetailText" />

# DetailText

<Canvas of={Stories.KitchenSink} sourceState="none" />

## Playground

<Canvas of={Stories.Playground} sourceState="shown" />
<Controls of={Stories.Playground} />

## Alternatives

- [BodyText](?path=/docs/typography-bodytext--docs) - For body text
- [Strong](?path=/docs/typography-strong--docs) - For strong importance
- [Em](?path=/docs/typography-em--docs) - For emphasis
- [Heading](?path=/docs/typography-heading--docs) - For heading-level text

## Size

The size prop is responsive, so you can render different sizes at different breakpoints.

```tsx
<DetailText size={{ mobile: 'sm', tablet: 'md', desktop: 'lg'}}>{...}</DetailText>
```

<Canvas of={Stories.TextSizes} />

## Color

The text color of `DetailText` can be set to either `text`, `valid` or
`invalid`. These colours will be overridden by the `inverted` prop.

## Inverted

When using `BodyText` on a darker background, specifically `uwPurple` &
`darkPurple`, use the `inverted` prop to ensure colour contrast.

<Canvas of={Stories.InvertedText} />

## API

This component is based on the `span` element and supports the following common props:

- Margin
- Text transform
- Text align
- Text wrap

| Prop      | Type                                                                  |
| --------- | --------------------------------------------------------------------- |
| `as`      | `"p" \| "div" \| "span"`                                              |
| `asChild` | `boolean`                                                             |
| `size`    | `Responsive<"sm" \| "md" \| "lg" \| "xl" \| "2xl" \| "3xl" \| "4xl">` |
