import { Meta, Canvas, ArgTypes } from '@storybook/blocks';

import * as Stories from './Fieldset.stories';

import { DocsHeader } from './storybook-components/DocsHeader';

<Meta title="Components / Fieldset" />

<DocsHeader componentName="Fieldset" stories={Stories} />

<Canvas of={Stories.Playground} />

## Alternatives

- [FormFieldGroup](?path=/docs/components-formfieldgroup--docs) - For default form field groups

## Grouping inputs

The `Fieldset` and `FieldsetLegend` components can be used together to group form inputs.
The `FieldsetLegend` should be the first child of the `Fieldset` for accessibility.

<Canvas of={Stories.GroupingInputs} />

## API

This component is based on the `fieldset` element and supports the following common props:

- Margin
