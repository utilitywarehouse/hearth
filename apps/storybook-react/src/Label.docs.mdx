import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';

import * as Stories from './Label.stories';

import { DocsHeader } from './storybook-components/DocsHeader';

<Meta title="Typography / Label" />

<DocsHeader componentName="Label" stories={Stories} />

## Playground

<Canvas of={Stories.Playground} sourceState="shown" />
<Controls of={Stories.Playground} />

## API

This component is based on the `label` element, and supports the following common props:

- Margin
- Text transform

| Prop                | Type                |
| ------------------- | ------------------- |
| `as`                | `"label" \| "span"` |
| `disabled`          | `boolean`           |
| `disableUserSelect` | `boolean`           |
