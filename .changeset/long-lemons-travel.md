---
'@utilitywarehouse/hearth-react': patch
---

🐛 [FIX]: Do not display the open icon in `AlertLink` as it clashes with the default icon

This change also removes the `hideOpenIcon` prop from `AlertLink` as it is no
longer needed.
