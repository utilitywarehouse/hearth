---
'@utilitywarehouse/hearth-tokens': patch
---

This change is to release the updated colour tokens. The light and dark colour scales have been combined into a single scale that can be used across both light and dark themes. This change also adds initial semantic tokens.
