---
'@utilitywarehouse/hearth-react': patch
---

This change eases the getting started process, and the tokens release process, by including the CSS Reset and tokens CSS in the React library, making them dependencies rather than peer dependencies.
